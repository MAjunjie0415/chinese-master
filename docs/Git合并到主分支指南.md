# Git 合并到主分支指南

## 📋 操作流程

### 当前状态
- 功能分支：`feature/wish-invite-system`
- 已提交并推送到远程
- 需要合并到主分支（`main` 或 `master`）

## 🚀 合并步骤

### 步骤1：确保所有更改已提交

```bash
# 检查是否有未提交的更改
git status

# 如果有未提交的更改，先提交
git add .
git commit -m "fix: 修复所有问题"
```

### 步骤2：切换到主分支

```bash
# 切换到主分支（可能是 main 或 master）
git checkout main
# 或
git checkout master
```

### 步骤3：拉取最新代码

```bash
# 确保主分支是最新的
git pull origin main
# 或
git pull origin master
```

### 步骤4：合并功能分支

```bash
# 合并功能分支到主分支
git merge feature/wish-invite-system

# 如果有冲突，解决冲突后：
git add .
git commit -m "merge: 合并许愿池和邀请码功能"
```

### 步骤5：推送到远程

```bash
# 推送到GitHub
git push origin main
# 或
git push origin master
```

### 步骤6：清理（可选）

```bash
# 删除本地功能分支（可选）
git branch -d feature/wish-invite-system

# 删除远程功能分支（可选）
git push origin --delete feature/wish-invite-system
```

## ⚠️ 注意事项

1. **合并前检查**：确保功能分支的代码已经测试通过
2. **冲突处理**：如果有冲突，需要手动解决
3. **Vercel自动部署**：推送到主分支后，Vercel会自动检测并部署

## 🔄 Vercel部署流程

1. 代码推送到GitHub主分支
2. Vercel自动检测到推送
3. 自动构建和部署
4. 部署完成后，新功能即可访问

## 📝 完整命令序列

```bash
# 1. 检查状态
git status

# 2. 切换到主分支
git checkout main

# 3. 拉取最新代码
git pull origin main

# 4. 合并功能分支
git merge feature/wish-invite-system

# 5. 推送到远程
git push origin main

# 6. 完成！Vercel会自动部署
```

